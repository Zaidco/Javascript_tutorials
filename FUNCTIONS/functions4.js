//handling objects

const user = {
    username: "Zaid",
    email: "zaid@google.com",
    sign :"Gemini"

}

function theObject(anyobject){
    console.log(`The username is ${anyobject.username} and the email is ${anyobject.email}`);
    }
theObject(user)